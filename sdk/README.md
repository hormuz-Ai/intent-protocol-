# INTP Solver SDK

The fastest way to build a solver on the Intent Protocol.

## Prerequisites

- Node.js ≥ 18
- A Vercel account (free)

## Usage

```bash
npx create-intent-solver my-solver
cd my-solver

vercel --prod
EOFEOF
